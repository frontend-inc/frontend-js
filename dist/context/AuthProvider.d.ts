import React from 'react';
declare type AuthProviderProps = {
    serverPath?: string;
    children: React.ReactNode;
};
declare const AuthProvider: (props: AuthProviderProps) => any;
export default AuthProvider;
