import { ResourceResponse } from '../types';
declare type UseCollectionParams = {
    name?: string;
};
declare const useCollection: (params: UseCollectionParams) => ResourceResponse;
export default useCollection;
